/**
 * Reusable layout primitives shared by every screen (ARCHITECTURE.md §1, L5).
 * Presentation only — these must never reach into services or data.
 */
export { Screen } from './Screen';
export { EmptyState } from './EmptyState';
export { Section } from './Section';
export { Row } from './Row';
